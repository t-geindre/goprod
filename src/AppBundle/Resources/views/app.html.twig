<script type="text/template" id="app-template">
    <div>
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="#">Goprod</a>
                </div>
                <div id="navbar" class="collapse navbar-collapse">
                    <ul class="nav navbar-nav">
                        <li class="active"><a href="#">Home</a></li>
                        <li><a href="#about">About</a></li>
                        <li><a href="#contact">Contact</a></li>
                    </ul>
                    <user-details
                        v-bind:authenticated="authenticated"
                        v-bind:user="user"
                        v-on:login="authenticate"
                        v-on:logout="clearAuth">
                    </user-details>
                </div>

            </div>

        </nav>
        <div class="container" id="container">
            <p v-if="authenticating">Identification en cours...</p>
            <p class="auth" v-on:click="authenticate" v-else v-if="!authenticated">Identification Github</p>
            <div v-else>
            main app
            </div>
        </div>
    </div>
</script>

<script type="text/javascript">
    window.GithubClient = {
        app: {
            clientId: "{{ github.client_id }}",
            scope: "repo"
        },
        url: {
            site: "{{ github.site }}",
            api: "{{ github.api }}",
            authProxy: "{{ path('auth') }}"
        }
    };
</script>

{% include '@App/component/user-details.html.twig' %}
